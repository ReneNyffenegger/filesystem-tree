A top-level txt file.

the directory release-notes contains files whose file names have version
numbers. this directory and its files were created to test
   ls -lv
   tree -v

./set-permissions.sh is a script that makes some files and directories
unreadable (so as to test error messages when using the shell command
find).
